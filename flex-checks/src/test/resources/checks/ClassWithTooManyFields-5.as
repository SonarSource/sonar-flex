class C { // Noncompliant {{Refactor this class so it has no more than 5 fields, rather than the 6 it currently has.}}
    var a;
    var b;
    public var c;
    internal var d;
    protected var e;
    private  var f;
}

class D {
    var a;
    var b;
}
