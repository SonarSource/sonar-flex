class C { // Noncompliant {{Refactor this class so it has no more than 2 public fields, rather than the 3 it currently has.}}
    var a;
    var b;
    public var c;
    internal var d;
    protected var e;
    private  var f;
}

class D {
    var a;
    var b;
}
