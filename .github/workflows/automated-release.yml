name: Automated Release
on:
  workflow_dispatch:
    inputs:
      short-description:
        description: "Short description for the REL ticket"
        required: true
        type: string
      sqc-integration:
        description: "Integrate into SQC"
        type: boolean
        default: true
      sqs-integration:
        description: "Integrate into SQS"
        type: boolean
        default: true
      branch:
        description: "Branch from which to do the release"
        required: true
        default: "master"
        type: string
      new-version:
        description: "New version to release (without -SNAPSHOT; if left empty, the current minor version will be auto-incremented)"
        required: false
        type: string
      bump-version:
        description: "Bump the version and open a PR for the next development iteration after the release"
        type: boolean
        default: true

jobs:
  release:
    name: Release
    uses: SonarSource/release-github-actions/.github/workflows/automated-release.yml@v1
    permissions:
      statuses: write
      id-token: write
      contents: write
      actions: write
      pull-requests: write
    with:
      project-name: "SonarFlex"
      plugin-name: "flex"
      jira-project-key: "SONARFLEX"
      rule-props-changed: false
      short-description: ${{ github.event.inputs.short-description }}
      new-version: ${{ github.event.inputs.new-version }}
      sqc-integration: ${{ github.event.inputs.sqc-integration == 'true' }}
      sqs-integration: ${{ github.event.inputs.sqs-integration == 'true' }}
      create-slvs-ticket: false
      create-slvscode-ticket: false
      create-sle-ticket: false
      create-sli-ticket: false
      branch: ${{ github.event.inputs.branch }}
      pm-email: "geoffray.adde@sonarsource.com"
      slack-channel: "squad-corelang-releases"
      release-automation-secret-name: "sonar-flex-release-automation"
      runner-environment: "github-ubuntu-latest-s"
      verbose: true
      use-jira-sandbox: false
      is-draft-release: false
      bump-version: ${{ github.event.inputs.bump-version == 'true' }}
      bump-version-tool: "maven"
